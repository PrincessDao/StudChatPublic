import 'package:flutter/material.dart';

class PublicTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Сообщество нгуэу'),
    );
  }
}